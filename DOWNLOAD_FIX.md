# 附件下载 403 修复记录

## 本次排查结论（2026-09-21）

用户已确认本次 403 在修正 Cookie 后解决，根因是 Cookie / 会话鉴权问题。Cookie 失效、格式不正确或未正确传入，都可能导致下载请求返回 403；本次反馈未进一步区分具体属于哪一种情况。

再次遇到同类问题时，按以下顺序检查：

1. 重新登录实际下载使用的账号，确认其有权访问附件，并获取有效 Cookie。
2. 检查云函数环境变量 `WPS_SID`、`KSO_SID`（以及实际请求需要时的 `WPS_UA`）：每项只填写对应 Cookie 的值，不包含引号、字段名、`Cookie:` 前缀或整段 Cookie 请求头。
3. 保存配置后，确认实际处理请求的新实例读取了这些变量，并在相应 WPS / 金山文档请求中携带凭据。仅刷新 OAuth Token 不会更新网页登录 Cookie。
4. 用一份新申请验证下载、解析和回写；若仍返回 403，结合脱敏后的失败阶段、HTTP 状态和请求 ID 排查账号权限等原因，不能把所有 403 都归因于 Cookie。

后续改进建议（本次仅补充文档，未实现）：增加 Cookie 配置缺失、格式异常和会话失效检测；结合 `userNotLogin` 等响应信号给出明确提示，例如“登录会话可能已失效，请重新登录并更新 Cookie 环境变量”，同时保留失败阶段和 HTTP 状态，避免仅显示 403。检测和日志不得输出 Cookie、Token 或签名 URL。

## Cookie 配置说明

后续已观察到：登录浏览器可以下载，无痕浏览器返回 `userNotLogin`；此前移除 Cookie 并重试的版本仍然返回 403。因此，下面的无 Cookie 修改记录仅代表第一轮尝试，不代表问题已解决。

在云函数环境变量中配置以下项目（只填写值，不添加引号、字段名或 `Cookie:`）：

| 环境变量 | 浏览器 Cookie 名称 | 说明 |
| --- | --- | --- |
| `WPS_SID` | `wps_sid` | 登录凭据，填写实际下载使用的有效值 |
| `KSO_SID` | `kso_sid` | 另一种登录凭据，可按实际请求填写；不是强制两项都有 |
| `WPS_UA` | `wpsua` | 可选设备凭据，实际请求携带时填写 |

元信息查询、下载地址申请和文件内容下载读取同一组环境变量。Cookie 仅发送到 HTTPS 的 `wps.cn`、`kdocs.cn` 及其子域名，其他存储域名不携带；OAuth Token 仍只用于 OpenAPI。未配置变量时保持原来的无 Cookie 行为。代码和日志不保存、输出这些值。

这是显式把提供的凭据用于两个域名的服务端配置，不表示浏览器会把 `.kdocs.cn` Cookie 自动发送到 `.wps.cn`。请使用与 OAuth 授权账号一致的凭据，以一次实际云端下载验证是否被接受。刷新 OAuth Token 不会刷新这些 Cookie；会话失效后需要更新环境变量。

部署包含 Cookie 配置支持的镜像，再保存环境变量并确保新实例使用该配置。选择一份新申请确认实际下载成功。此前本地测试使用模拟响应；本次修复成功的结论来自用户反馈，未另行执行线上验证。

---

## 第一轮修改与验证记录（历史）

以下保留当时的实现、测试结果和未验证事项；本次 Cookie 问题的解决状态以上方排查结论为准。

基线：`auto-hr-docker`，提交 `3c42415fa2878c186bf1cd794a4454b3dec5b1d6`。

## 已确认的问题与修改

- 原代码申请下载地址时使用 OAuth Token，下载文件时却添加仓库内写死的网页登录 Cookie。移除这些 Cookie，完整保留接口返回的临时 URL，不另行转发 OAuth Token 到存储域名。
- 下载地址申请遇到 408、429 或部分 5xx，文件下载遇到这些状态或 401/403、连接超时、传输中断、空文件时，最多尝试 3 次，间隔 1 秒、2 秒。每次重新申请下载地址。元信息查询不增加重试；接口层的 401/403 不当作下载链接失效进行重试。
- 文件先写入独立临时文件，下载完成且非空后才替换目标文件。可用时校验 Content-Length；失败清理临时文件。
- 错误区分元信息查询、下载地址申请、文件内容下载三个阶段，不再输出 Token、Cookie、签名 URL。
- 下载或获取 Token 失败后跳过盖章，回写和失败通知使用原始错误，避免 `local_path` 或未初始化变量引起的二次错误。

## 验证与边界

执行：`python -B -m unittest discover -s tests -v`。

测试用模拟 HTTP 响应覆盖 403 重试恢复、持续 403、接口拒绝、服务暂时故障、超时、中断、空文件、传输不完整、敏感信息脱敏，以及真实回调函数的成功/失败路径。云端回写、邮件和盖章依赖均已模拟，测试不触发这些线上操作。

测试结果：30 项中 29 项通过，1 项原有的真实 PDF 样本测试因未提供样本而跳过。

尚未使用线上有效凭据请求实际失败附件，也未构建/部署 Docker 镜像。写死的 Cookie 是已确认的代码缺陷，但它是否为此次持续 403 的唯一根因尚未实测确认。移除 Cookie 的实现依赖返回 URL 本身可用于下载；如果该存储地址仍要求额外登录凭据或受权限策略限制，这个修改不会自动解决授权问题。

WPS [单文件下载接口说明](https://open.wps.cn/documents/app-integration-dev/wps365/server/yundoc/file/get-file-download)确认返回下载 URL；其[批量下载说明](https://open.wps.cn/documents/app-integration-dev/wps365/server/yundoc/file/batch-download-files)另说明公网网关可能核验与申请阶段相同的登录凭据。不能据此断言单文件 OAuth 链路一定支持无 Cookie 下载，也不能盲目添加过期 Cookie 或把 Bearer Token 发给任意存储域名。

后续部署后应选择一份新申请验证：下载完成、正常解析、回写正确。若仍失败，保留脱敏后的阶段、HTTP 状态和云函数请求 ID，与 WPS 接口调用日志对照。自动重试仅在当前任务内部进行，不会重放过去已失败的申请。

本记录描述代码修改与本地验证结果；尚未部署到线上。
